{% extends 'base.html.twig' %}

{% block title %}Sortir.com | Ajouter des utilisateurs{% endblock %}

{% block body %}


    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Ajouter des utilisateurs par CSV</h3>
            </div>
            <div class="card-body">

                {{ form_start(form) }}


                {{ form_label(form.file) }}
                {{ form_widget(form.file, {'attr': {'class': 'form-control'}}) }}

                <br />

                <button class="btn btn-primary">Ajouter les utilisateurs</button>
                <a href="{{ path('main') }}"><button type="button" class="btn">Cancel</button></a>


                {{ form_end(form) }}


            </div>
        </div>
    </div>
{% endblock %}
